# ANTHROPIC API Key（実LLMモード使用時に必要。モックモードは不要）
ANTHROPIC_API_KEY=

# EDINET API Subscription Key（EDINET書類検索API使用時に必要）
EDINET_SUBSCRIPTION_KEY=

# API認証キー（本番デプロイ時に設定。未設定時は認証スキップ＝開発モード）
# 生成例: python3 -c "import secrets; print(secrets.token_hex(32))"
API_KEY=
