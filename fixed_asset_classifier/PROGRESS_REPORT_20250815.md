# 開発進捗レポート (2025/09/04)

## 1. 本日の進捗サマリー

本セッションでは、v2解析エンジンの動作検証と、その過程で発見された複数のバグ修正を実施した。

*   **v2エンジンの実行とデバッグ:**
    *   ユーザーからの的確な指摘に基づき、実行環境の差異に起因する環境変数の設定問題を解消。
    *   v2エンジンの起動を妨げていた複数のバグ（`TypeError`, `AttributeError`）を特定し、すべて修正した。
    *   これにより、**v2解析パイプラインがエラーなく最後まで実行されることを確認**。テキスト層が破損したPDFを検知し、OCRフォールバックがトリガーされる基本動作が確立された。

*   **現状の課題:**
    *   パイプラインは完走するものの、最終的な品名（`description`）の文字化けが解消されていない。
    *   原因は、OCRで読み取った正しい日本語テキストを、元の文字化けテキストに上書きする品名復元ロジック（`attach_desc_from_line_text`）が、意図通りに機能していないためと特定。

## 2. 次のステップ

*   品名復元ロジックの精度向上に注力する。具体的には、後述の「ターゲット・リコンストラクション戦略」に基づき、`attach_desc_from_line_text`関数のデバッグと機能強化を行う。

---

# 開発進捗レポート (2025/08/31)

## 1. 本日の進捗サマリー

本セッションでは、一連のデバッグ作業の集大成として、システムのPDF読取精度を抜本的に改善する**v2解析エンジン**を実装した。

*   **根本原因の特定と最終解決策:**
    *   PDFの日本語テキスト層が破損している場合、`INVOICE_PROCESSOR`がこれを優先してしまい、文字化けしたテキストを抽出してしまう問題（`ja_char_ratio=0.0`）を根本原因として特定した。
    *   最終的な解決策として、構造解析に強い`INVOICE_PROCESSOR`と、画像からの日本語OCRに強い`OCR_PROCESSOR`を組み合わせる**「ハイブリッド・フォールバック方式」**を確立した。
    *   具体的には、まず`INVOICE_PROCESSOR`で解析し、日本語率が低い場合にのみ、PDFをTIFF画像に変換して`OCR_PROCESSOR`で再解析。最初の解析で得た構造情報と、OCRで得たクリーンな日本語テキストを組み合わせて品名を復元する。

*   **v2エンジンの実装:**
    *   上記ロジックを実装した`document_analyzer_v2.py`と、プロセッサを正しく使い分ける`docai_wrapper_v2.py`を新規作成。
    *   LLMの呼び出しを、より安定した`gemini-2.5-pro`をデフォルトとするフォールバック対応版`invoice_inferencer.py`に更新。

*   **運用性の向上:**
    *   `main.py`を修正し、環境変数`ANALYZER_VERSION`でv1（安定版）とv2（最新版）を切り替えられるようにした。
    *   v2エンジンを簡単に試せるよう、`run_ui_v2.bat`を新規作成した。
    *   一連の変更を`MANUAL.md`と`architecture.md`に反映した。

## 2. 次のステップ

*   v2エンジンを有効化し、これまで問題となっていたPDFファイルで、品名抽出精度が改善されたかを最終確認する。

---

# 開発進捗レポート (2025/08/30)

## 1. 本日の進捗サマリー

本セッションでは、Streamlitアプリで明細が表示されない問題のデバッグから開始し、その過程で発覚した複数の根本原因を修正。最終的に、ユーザーからの高度なフィードバックに基づき、システムのコアである`document_analyzer.py`の安定性と精度を向上させるための、大規模なリファクタリングを実施した。

*   **初期課題とデバッグ:**
    *   UIでの明細表示欠落の問題を追い、`rule_engine.py`の戻り値修正、`docai_wrapper.py`のAPIパラメータ修正、GCP Quota Project設定など、複数の問題を特定・修正した。
    *   一連の問題の根本原因として、`INVOICE_PROCESSOR`が`OcrConfig`をサポートしていないため、APIエラーが発生しデータが抽出されていなかったことを特定した。

*   **`document_analyzer.py`の全面リファクタリング:**
    *   ユーザー提供の優れたコードに基づき、複数プロセッサに依存する複雑なアプローチから、`INVOICE_PROCESSOR`単体で最大限の性能を引き出す、より堅牢な「単一路線」のアプローチに設計を刷新した。
    *   **`attach_desc_from_line_text`（行内テキストフォールバック）を導入**: 金額の左側にあるテキストから品名を復元する強力なロジックを追加し、LLMなしでも品名抽出率を大幅に向上させる基盤を構築した。
    *   テキスト正規化(`_normalize_desc`)や、より高度な重複除去ロジックを導入し、データ品質を改善した。

*   **LLMバックフィル機能の修正:**
    *   `invoice_inferencer.py`内の`tool_config`のモードを`ANY`から`AUTO`に修正し、LLMによる品名補完機能が正しく動作するようにした。

*   **リファクタリング中のバグ修正:**
    *   実装過程で発生した複数の`NameError`（`AnalyzerConfig`, `HAS_VISION`, `prep`）を特定し、すべて修正済み。

## 2. 次のステップ

*   新しいアーキテクチャでの動作を再検証し、品名抽出の精度向上を確認する。
*   ユーザーからのフィードバックに基づき、残りの改善タスク（横並びテキストからの品名再構築など）に着手する。

---

# 開発進捗レポート (2025/08/29 - セッション2)

## 1. 本日の進捗サマリー

本セッションでは、既存アーキテクチャのドキュメント化、動作検証、デプロイ準備、および最優先課題の根本原因特定を完了した。

- **アーキテクチャの可視化とドキュメント化:**
  - PDF解析のパイプラインを「5層構造PDF解析アーキテクチャ」として定義し、`architecture.md`に図付きで出力。
  - ルール判定部分を「ハイブリッド判定アーキテクチャ」として定義し、`rules_architecture.md`に図付きで出力。

- **コアロジックの動作検証:**
  - `asset_advisor.py` を実行し、バックエンドの判定ロジックが正常に動作し、`fixed_asset_classification_results.csv` に結果が出力されることを確認。

- **Cloud Runデプロイ準備:**
  - `docs/cloud_run_deployment_guide.md` に基づき、更新デプロイ手順を明確化。
  - 最新のコードをコンテナイメージとしてビルドし、Google Container Registryへのプッシュに成功。
  - ユーザーがAPIキーを埋め込んで実行するだけの最終的な `gcloud run deploy` コマンドを準備完了。

- **BUG-001の根本原因を特定:**
  - `InvoiceInferencer`のエラーを調査。`invoice_inferencer.py`と`requirements.txt`を分析した結果、原因が`google-generativeai`ライブラリのAPI仕様変更（`response_schema`の渡し方）であることを特定した。
  - `tools`引数を使用する具体的な修正方針を確立。

## 2. 次のステップ

- 次回セッションの冒頭で、`BUG-001`の修正パッチを適用する。

---

# 開発進捗レポート (2025/08/29)

## 1. 本日の進捗と課題

本日は、システムの基盤となるコアロジックの統合とデバッグに注力した。

- **主要コンポーネントの統合:**
  - `rules_linter.py`の作成と`rules.json`の構造チェック（問題なし）。
  - `rule_engine_strict.py`の作成（安全で厳格なルールエンジン）。
  - `asset_advisor_debug_patch.py`の作成（詳細デバッグ用）。
  - `asset_advisor.py`を、新しい`RuleEngineStrict`を使用するように改修。
  - `main.py`を、`SafetyNet`と`RobustDocumentAnalyzer`を呼び出すように最終統合。

- **エンドツーエンドテストの実行:**
  - 複数回のデバッグと修正（エンコーディング問題、`KeyError`、`deepcopy`インポート漏れなど）を経て、**主要なパイプラインがエンドツーエンドで動作することを確認。**
  - PDF解析、グルーピング、ルールエンジンによる分類が連携して機能している。

- **現在の課題（LLM推論のブロック）:**
  - `InvoiceInferencer`が、`GenerativeServiceClient.generate_content()`の`response_schema`引数に関するエラーで失敗している。これは`google.generativeai`ライブラリのバージョン互換性、または引数の渡し方の問題と推測される。
  - この問題により、品名が欠落している項目に対するLLMによる自動補完が機能していない。

## 2. WBSの更新

以下のタスクを「完了」に更新。

- **No.34:** ルールエンジン強化 (priority/stop_on_match)
- **No.35:** 新ルール体系の適用
- **No.36:** 多チャネル合議グルーピング実装 (bundler_v2)

---

# 開発進捗レポート (2025/08/28)

## 1. アーキテクチャの進化と主要な進捗

前回のレポート以降、議論を通じてアーキテクチャが大幅に進化し、実装が進行した。主な進捗は以下の通り。

- **LLM判定の再現性・信頼性向上 (実装完了):**
  - LLMの出力を固定化するためのJSONスキーマ (`schemas.py`) を定義。
  - 温度0、スキーマ検証、リトライ、安全なフォールバック機構を持つ決定的ラッパー (`llm_classifier.py`) を実装。これにより、LLM判定のブレをなくし、監査対応可能なレベルまで信頼性を向上させた。

- **資産グルーピング機能の基盤実装 (実装完了):**
  - 複数行にまたがる品目を「資産単位」で束ねるための基盤として、キーワードベースのグルーピング機能 (`bundler.py`) を実装。
  - `main.py`のワークフローに組み込み、データ抽出後にグルーピング処理が実行されるように変更した。

## 2. 主要な設計決定と今後の方向性

実務での利用を想定し、以下の通り、より高度なアーキテクチャへの移行を決定した。

- **グルーピング機能の高度化:**
  - **設計決定:** 現行のキーワードベースから、複数の特徴量（テキスト類似度、金額比率、意味ベクトル等）を統合的に評価する**「8チャネル合議（Ensemble）モデル」**へと進化させる。
  - **ステータス:** 設計完了。次ステップで実装予定 (`bundler_v2.py`)。

- **ルールエンジンの強化:**
  - **設計決定:** ルールに`priority`（優先度）と`stop_on_match`（一致時に停止）の概念を導入し、より複雑な判定ロジックを明確に記述できるようにする。
  - **ステータス:** 設計完了。次ステップで実装予定。

- **トレーサビリティの抜本的強化:**
  - **設計決定:** すべての判定結果に、`line_uid`, `bundle_id`, `rule_id`, `prompt_hash`といった一意なIDと根拠情報を付与し、完全な追跡可能性を確保する。
  - **ステータス:** 設計完了。各コンポーネントに順次実装予定。

- **OCR精度のさらなる向上策:**
  - **設計決定:** Document AIで精度が不十分な場合、PaddleOCRやTesseractといった複数のOCRエンジンを並走させ、結果を合議させるパイプラインを将来的な拡張機能として構想。
  - **ステータス:** 構想段階。

---

# 開発進捗レポート (2025/08/22)

## 1. 現在の到達点

「固定資産判定エージェント」の第一段階である `DocumentAnalysisAgent` の技術実証(PoC)を完了しました。

**達成事項:**
- 見積書PDFをインプットとして、Google Cloud Document AIを呼び出し、記載されている品目、数量、金額などの明細を構造化データ(JSON)として抽出することに成功。
- `rules.json`の強化（未使用ノードの再接続、キーワード拡張、金額判定ロジックの明確化）が完了。
- `asset_advisor.py`がルールエンジンと統合され、ハイブリッド判定ロジックが機能している。
- 自動テストスイートが作成され、全テストがパスする状態になった。

## 2. 今回のセッションでの主な進捗 (2025/08/15 - 2025/08/22)

前回のレポート以降、以下の主要なタスクを完了しました。

- **ルールエンジンの基盤強化とバグ修正:**
  - `rule_engine.py`内の`TypeError`（`RuleEngine.navigate_to()`の引数問題）を修正し、テスト実行のブロックを解消しました。
  - `rules.json`の評価式における`keyword_sets`の参照構文エラーを修正しました。
- **ユーザー入力の堅牢化とUX改善 (WBS 1.7):**
  - 支出金額および過去取得価額が「不明」な場合のロジックを`rules.json`に追加しました。これにより、ユーザーは不明な値を入力できるようになり、適切なガイダンスに誘導されます。
  - `asset_advisor.py`の`_drive_rule_engine`を修正し、これらの新しい対話フローに自動で対応できるようにしました。
- **使用可能期間延長に関するauto_logicの改善 (WBS 1.5):**
  - `rules.json`の`q3_revised`ノードに、より詳細な自動判定ロジック（`keyword_map`タイプ）を導入しました。これにより、「明確に延長される」「延長は期待するが不確実」「延長されない」の3つのケースをキーワードで自動判定できるようになりました。
  - `asset_advisor.py`を修正し、新しい`keyword_map`タイプの`auto_logic`を処理できるようにしました。
- **フロー/遷移構造の最適化 (WBS 1.10):**
  - `rules.json`の初期評価フローを最適化し、金額判定よりも先に品目タイプ（ソフトウェア、車両など）の判定を行うように変更しました。
- **10万円未満支出の分類詳細化 (WBS 1.6):**
  - フロー最適化後、10万円未満の支出に対して、広告宣伝費や支払手数料など、より具体的な勘定科目をキーワードで自動判定するロジックを`rules.json`に追加しました。
- **キーワード/NLP面の高度化 (WBS 1.9):**
  - `rules.json`の`auto_logic`に「否定キーワード」の概念を導入しました。これにより、特定のキーワードが含まれていても、否定キーワードがあればマッチングを無効にできるようになり、ルールの表現力が向上しました。
  - `asset_advisor.py`の`_match_keywords`関数を修正し、否定キーワードを処理できるようにしました。
  - `keywords.json`に既存キーワードの同義語を多数追加し、マッチング精度を向上させました。
- **実装品質・保守の改善 (WBS 1.13):**
  - `rules.json`内のキーワード定義をトップレベルの`keyword_sets`に集約し、各ノードからは参照する形式にリファクタリングしました。
- **テストスイートの強化:**
  - 上記の全ての変更に対応するため、既存のテストケースを更新し、新しいロジックパスを検証するテストケースを多数追加しました。現在、**全21件のテストが正常にパス**しています。
- **Cloud Runデプロイ環境の整備 (WBS No.12):**
  - `Dockerfile`の作成、`requirements.txt`の更新、`.gitignore`の整備を完了しました。
  - Cloud Runへのデプロイ手順を`docs/cloud_run_deployment_guide.md`にまとめました。
- **デモ動画スクリプト骨子の作成 (WBS No.8):**
  - デモ動画のスクリプト骨子を作成し、`docs/demo_video_script_outline.md`に保存しました。

## 3. 使用した主要ツール

今回のセッションでも、以下のツールを駆使して開発を進めました。

- **`run_shell_command`**: スクリプトのテスト実行、エラー原因の特定に不可欠でした。
- **`replace`**: コードやルール定義の修正に多用しました。
- **`write_file`**: `rules.json`やテストファイルの広範囲なリファクタリングに活用しました。
- **`read_file` / `read_many_files`**: コードやルール定義の現状把握に継続的に使用しました。

## 4. 次のステップ

- WBS 1.8 (B) 会計ロジック/ポリシー面の詳細化 に着手する予定です。具体的な作業内容については、ユーザーからの追加の指示を待っています。

## 5. 今後の課題（備忘録）

- OCR/PDF抽出の精度問題
- デプロイの不確実性
- LLMの安定性とコスト

## 6. 明日以降の課題

今回の開発セッションで多くの進捗がありましたが、明日以降の課題としては、主に以下の点が挙げられます。

1.  **Cloud Runへのデプロイと動作確認 (WBS No.13):**
    *   デプロイ環境は整備済みですが、実際のデプロイと動作確認は未着手です。ハッカソン提出に必須のタスクであり、デモ動画作成の前提となります。
2.  **デモ動画の作成 (WBS No.8):**
    *   スクリプト骨子は作成済みですが、ナレーション生成、画面録画、動画編集が残っています。
3.  **Zenn記事最終版＆動画編集 (WBS No.14):**
    *   デモ動画と連携するZenn記事の最終化と動画編集が残っています。
4.  **OCR/PDF抽出の精度問題:**
    *   引き続き、実世界の多様なPDFに対するDocument AIの抽出精度は課題です。
5.  **LLMの安定性とコスト:**
    *   LLMの判断の安定性やAPIコストは、長期的な運用を考えると課題となります。
6.  **会計ロジック/ポリシー面の詳細化 (WBS No.1.8):**
    *   優先度Bのタスクであり、時間があれば着手したい項目です。
7.  **GCPプロジェクトIDの環境変数化とセキュリティ強化:**
    *   `config.json`からGCPプロジェクトIDを削除し、環境変数で管理する。
    *   関連コード（`main.py`, `asset_advisor.py`など）を環境変数から読み込むように修正する。
    *   開発環境用の`.env`ファイルを作成し、`.gitignore`で確実に除外する。

---

# 開発進捗レポート (2025/09/07)

## 1. 本日の進捗サマリー

本セッションでは、ワークブックの修復とテストの強化、および最終的なGit操作を完了した。

*   **ワークブックの修復と堅牢化:**
    *   `useful_life.xlsx` を再構築し、`bias_rules` シートを正常に含めるようにした。
    *   `fixed_asset_classifier/useful_life_excel.py` を修正し、`bias_rules` シートが存在しない場合でも、例外を発生させずに処理を継続できるよう堅牢化した。
*   **テストと検証の強化:**
    *   ゴールデンファイルを再生成し、スナップショットテストがすべてパスすることを確認した。
    *   `tests/test_useful_life_excel.py` を新規追加し、ワークブックの構造と耐用年数解決ロジックの回帰テストを導入した。テスト内のパス計算の問題も修正済みである。
*   **コードベースのメンテナンス:**
    *   `.gitignore` を更新し、ローカルの一時ファイルやバックアップファイルがGitに追跡されないようにした。
    *   `README.md` を更新し、クイックスタートガイド、ワークブックの仕様、運用上のヒントなどを追記した。
*   **Git操作の完了:**
    *   上記すべての変更をローカルリポジトリにコミットした。
    *   `hackathon-final` ブランチをリモートリポジトリ (`https://github.com/Majiro-ns/fixedassets.git`) にプッシュした。
    *   `useful-life-merge` タグをリモートリポジトリにプッシュした。
*   **環境問題の克服:**
    *   `&&` によるコマンド連結、`python -c` の引用符解釈、`del` コマンドの不安定性など、シェル実行環境に起因する複数の問題を、単一コマンドと絶対パス指定、ファイルベースのコミットメッセージを用いることで克服した。

## 2. 次のステップ

*   Cloud Runへのデプロイと動作確認 (WBS No.13) に着手する予定である。
*   デモ動画の作成 (WBS No.8) に着手する予定である。
*   Zenn記事最終版＆動画編集 (WBS No.14) に着手する予定である。

---

# 開発進捗レポート (2025/09/07)

## 1. 本日の進捗サマリー

本セッションでは、ワークブックの修復とテストの強化、および最終的なGit操作を完了した。

*   **ワークブックの修復と堅牢化:**
    *   `useful_life.xlsx` を再構築し、`bias_rules` シートを正常に含めるようにした。
    *   `fixed_asset_classifier/useful_life_excel.py` を修正し、`bias_rules` シートが存在しない場合でも、例外を発生させずに処理を継続できるよう堅牢化した。
*   **テストと検証の強化:**
    *   ゴールデンファイルを再生成し、スナップショットテストがすべてパスすることを確認した。
    *   `tests/test_useful_life_excel.py` を新規追加し、ワークブックの構造と耐用年数解決ロジックの回帰テストを導入した。テスト内のパス計算の問題も修正済みである。
*   **コードベースのメンテナンス:**
    *   `.gitignore` を更新し、ローカルの一時ファイルやバックアップファイルがGitに追跡されないようにした。
    *   `README.md` を更新し、クイックスタートガイド、ワークブックの仕様、運用上のヒントなどを追記した。
*   **Git操作の完了:**
    *   上記すべての変更をローカルリポジトリにコミットした。
    *   `hackathon-final` ブランチをリモートリポジトリ (`https://github.com/Majiro-ns/fixedassets.git`) にプッシュした。
    *   `useful-life-merge` タグをリモートリポジトリにプッシュした。
*   **環境問題の克服:**
    *   `&&` によるコマンド連結、`python -c` の引用符解釈、`del` コマンドの不安定性など、シェル実行環境に起因する複数の問題を、単一コマンドと絶対パス指定、ファイルベースのコミットメッセージを用いることで克服した。

## 2. 次のステップ

*   Cloud Runへのデプロイと動作確認 (WBS No.13) に着手する予定である。
*   デモ動画の作成 (WBS No.8) に着手する予定である。
*   Zenn記事最終版＆動画編集 (WBS No.14) に着手する予定である。

